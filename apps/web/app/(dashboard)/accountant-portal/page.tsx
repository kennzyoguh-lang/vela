import { Building2 } from "lucide-react";
import { ModulePlaceholder } from "@/components/templates/ModulePlaceholder";

export default function AccountantPortalPage() {
  return (
    <ModulePlaceholder
      icon={Building2}
      title="Accountant Portal is coming soon"
      description="Multi-client dashboards and the invite-only client link model ship in a later phase."
    />
  );
}
